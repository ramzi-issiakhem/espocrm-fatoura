define('custom:views/lead/convert', ['crm:views/lead/convert'], function (Dep) {
    return Dep.extend({
        // Your custom logic here
        // For example:
         setup: function () {
             //Dep.prototype.setup.call(this);
             // Additional setup logic
         },
    });
});